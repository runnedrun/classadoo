extends layout

block content
  .container-fluid
    .row.text-center
      h2.col-md-4.col-md-offset-4 Create new class
    .row
      .col-md-4.col-md-offset-4.text-center
        form.login-form(action="/classes" method="POST")
          .form-group
            label(for="name")
            input.form-control#name(name="name" type=text placeholder="Create Level 1" maxLength="40")                              
          button.btn.btn-default(type="submit") Submit